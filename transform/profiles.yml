atlas:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'atlas') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'atlas') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'atlas') }}"
      schema: public
      threads: 4
